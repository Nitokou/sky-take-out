<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CategoryMapper">
    <insert id="add">
        INSERT INTO category(type, name, sort, status, create_time, update_time, create_user, update_user)
        VALUES (#{type},#{name},#{sort},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
    <!--   parameterType 是干什么的？	告诉 MyBatis SQL 的参数类型是什么-->
<!--    写动态sql update语句 后续的更新语句全部放到这里-->
    <update id="update">
        update category
        <set>
            <if test="type != null">type = #{type}, </if>
            <if test="name != null">name = #{name}, </if>
            <if test="sort != null">sort = #{sort}, </if>
            <if test="status != null">status = #{status}, </if>
            <if test="updateTime != null">update_time = #{updateTime}, </if>
            <if test="updateUser != null"> update_user = #{updateUser}</if>
        </set>
        where id = #{id}
    </update>

    <select id="pageQuery" resultType="com.sky.entity.Category">
        select * from category
        --         动态sql语句
        <where>
            <if test="name != null and name != '' ">
                and name like concat('%', #{name}, '%')
            </if>
            <if test="type != null and type != '' ">
                and type like concat('%', #{type}, '%')
            </if>
        </where>
        order by create_time desc
    </select>
    <select id="queryTypeCategory" resultType="com.sky.entity.Category">
        select * from category
        --         动态sql语句
        <where>
            <if test="type != null">
               type = #{type}
            </if>
        </where>
    </select>
</mapper>